[pytest]
testpaths = tests
# Every test coroutine is run by pytest-asyncio without needing a marker.
asyncio_mode = auto
addopts = -ra --strict-markers --strict-config
# Warnings are failures: they are how deprecations get noticed before they break.
filterwarnings = error
