# Development and CI dependencies (includes runtime).
#   pip install -r requirements-dev.txt

-r requirements.txt

# Tests
pytest~=9.1.1
pytest-asyncio~=1.4.0
pytest-cov~=7.1.0
httpx~=0.28.1

# Lint / format / types
ruff~=0.16.3
mypy~=2.3.1
